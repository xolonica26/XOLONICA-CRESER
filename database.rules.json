{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$userId": {
        ".read": "auth != null && (data.child('email').val() === auth.token.email || auth.token.email === 'byoscarelieser@gmail.com' || auth.token.email === 'xolonica26@gmail.com')",
        ".write": "auth != null && (newData.child('email').val() === auth.token.email || auth.token.email === 'byoscarelieser@gmail.com' || auth.token.email === 'xolonica26@gmail.com')"
      }
    },
    "mood_logs": {
      "$user": {
        "$logId": {
          ".read": "auth != null && data.child('userEmail').val() === auth.token.email",
          ".write": "auth != null && newData.child('userEmail').val() === auth.token.email"
        }
      }
    },
    "journal_entries": {
      "$user": {
        "$entryId": {
          ".read": "auth != null && data.child('userEmail').val() === auth.token.email",
          ".write": "auth != null && newData.child('userEmail').val() === auth.token.email"
        }
      }
    },
    "resources": { ".read": true, ".write": "auth != null && (auth.token.email === 'byoscarelieser@gmail.com' || auth.token.email === 'xolonica26@gmail.com')" },
    "emergency_contacts": { ".read": true, ".write": "auth != null && (auth.token.email === 'byoscarelieser@gmail.com' || auth.token.email === 'xolonica26@gmail.com')" },
    "cms_content": { ".read": true, ".write": "auth != null && (auth.token.email === 'byoscarelieser@gmail.com' || auth.token.email === 'xolonica26@gmail.com')" }
  }
}
